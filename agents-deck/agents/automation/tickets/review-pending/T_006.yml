id: T_006
title: "Configurer n8n — alertes Elasticsearch → Microsoft Teams"
agent: Automation-agent
phase: 1
priority: high
category: feature
status: review-pending
created: "2026-06-29"
acceptance:
  - "Vérifier que n8n est accessible sur http://localhost:5678"
  - "Créer le workflow 'NetWatch Alertes' dans n8n"
  - "Nœud Elasticsearch : polling toutes les 5 min, filtre severity:high OR critical, last 5 min"
  - "Nœud Microsoft Teams : webhook configuré, message formaté avec IP source, règle, timestamp, lien portail"
  - "Tester avec une alerte simulée (`python3 simulate-traffic.py`) → notification Teams reçue"
  - "Exporter le workflow en JSON dans scripts/automation/n8n-alertes-teams.json"
  - "Documenter dans docs/n8n-setup.md : URL, credentials ES, webhook Teams, test"
delivery:
  summary: |
    n8n intégré dans docker-compose NetWatch (port 5678, auth admin/netwatch2026, volume persistant n8n-data).
    Workflow 'NetWatch Alertes' créé : polling ES toutes les 5 min sur indices suricata-*/snort-*,
    filtre severity high/critical, notification Teams via Adaptive Card (IP source, règle, timestamp, lien portail).
    Livrables : n8n-alertes-teams.json (workflow exporté), deploy-n8n-workflow.sh (import auto),
    test-pipeline.py (test end-to-end), docs/n8n-setup.md. Webhook Teams à configurer par l'opérateur.
  technical: |
    - docker-compose.yml : service n8n avec extra_hosts host.docker.internal→host-gateway pour accès ES local
    - scripts/automation/n8n-alertes-teams.json : workflow n8n 3 nœuds (Schedule→ES Query→Teams HTTP)
    - scripts/automation/deploy-n8n-workflow.sh : import via API REST n8n POST /rest/workflows
    - scripts/automation/test-pipeline.py : injecte alerte ES + vérifie pick-up workflow
    - docs/n8n-setup.md : guide complet setup, credentials, webhook Teams, commandes test
  delivered_at: "2026-06-30T10:00:00Z"
