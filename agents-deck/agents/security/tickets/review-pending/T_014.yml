id: T_014
title: "Score de risque composite IOC par IP"
agent: Security-agent
phase: 3
priority: high
category: feature
status: review-pending
created: "2026-06-30"
started: "2026-06-30"
finished: "2026-06-30"
branch: "agent/security/active"
acceptance:
  - "scripts/security/ioc-score.py : calcule score 0-100 par IP source depuis ES"
  - "Score = nb_alertes×1 + sévérité(critical×10, high×5, medium×2) + moteurs_distincts×15 + abuse_score/10 + mitre_ttps_uniques×8 — cap 100"
  - "Seuils : critical≥80, high≥60, medium≥40, low<40"
  - "Output JSON : [{ip, score, level, alerts_count, engines:[], top_rule, enrichment:{}, mitre_ttps:[]}] trié par score desc"
  - "Route /api/ioc-scores dans portal/app.py — exécute ioc-score.py, retourne JSON (TTL cache 5 min)"
  - "Mettre à jour scripts/security/ioc-graph.py : ajouter risk_score dans les nodes IP"
  - "docs/ioc-scoring.md : formule, seuils, exemples"
delivery:
  summary: |
    Score composite IOC livré. ioc-score.py interroge suricata-*, snort-* et zeek-*
    sur la période configurée (--days, défaut 1j), agrège par IP source et calcule
    un score 0-100 (alertes + sévérité pondérée + bonus moteurs distincts × 15 +
    bonus MITRE uniques × 8 + abuse_score/10 depuis cache AbuseIPDB).
    Route /api/ioc-scores ajoutée dans portal/app.py avec cache mémoire + fichier TTL 5 min.
    ioc-graph.py enrichit désormais chaque nœud ip_src/ip_dst avec risk_score, risk_level,
    risk_engines, risk_top_rule, risk_mitre_ttps via subprocess ioc-score.py.
    Demo : 185.220.101.46 → 80/critical (Tor, 2 moteurs, 2 MITRE), 10.0.0.55 → 79/high.
  technical: |
    - scripts/security/ioc-score.py : script principal (stdlib + elasticsearch-py optionnel,
      argparse --days/--output/--threshold/--cache/--demo, fallback demo data intégré,
      poids Suricata severity 1-4, Snort priority 1-3, Zeek medium=2)
    - portal/app.py : route GET /api/ioc-scores ajoutée (~70 lignes), cache in-memory
      + fichier ioc-scores-cache.json, TTL 300s, fallback stale cache sur timeout 60s
    - scripts/security/ioc-graph.py : fonctions load_risk_scores() + enrich_nodes_with_scores()
      ajoutées, main() appelle ioc-score.py en subprocess après build_graph(), flag --no-scores
      pour désactiver, import subprocess + Path ajoutés
    - docs/ioc-scoring.md : formule détaillée, tableau poids par moteur, seuils, format JSON
      complet, exemples chiffrés, pipeline ioc-graph → ioc-score → ioc-enrich
  delivered_at: "2026-06-30T13:45:00Z"
